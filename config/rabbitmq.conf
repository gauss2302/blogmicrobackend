# RabbitMQ configuration used by docker-compose.
# Keep the built-in 'guest' user loopback-only (the secure default). Do not set
# loopback_users.guest = false — that would let guest authenticate over the network.
consumer_timeout = 1800000
